# Dockerfile.dev
FROM node:20-alpine

WORKDIR /app

# Dépendances (couche cachée séparément)
COPY package*.json ./
RUN npm install

# Config TypeScript + sources (inclut src/mocks/**/*.json)
COPY tsconfig.json ./
COPY src/ ./src/

EXPOSE 5400

CMD ["npm", "run", "dev"]
